# AGENTS.md — MSA 项目永久工作规则

本文件适用于所有参与 MSA（Multi-Scale Structure Alignment）项目的人工成员、ChatGPT、Codex Agent 与其他自动化代理。

## 1. 核心研究纪律

1. **禁止未来信息泄漏（lookahead / future leak）。**
2. 所有结构对象必须区分 `OriginTime` 与 `ConfirmTime`。
   - `OriginTime`：原始极值、结构或区域实际发生的时间。
   - `ConfirmTime`：算法在实时条件下最早可以确认该结构的时间。
3. 允许历史图形从 `OriginTime` 开始显示结构，但任何“有效”“确认”“可交易”“可提醒”状态只能从 `ConfirmTime` 开始生效。
4. 禁止为了历史图更漂亮，将后来确认的交易信号回填到过去并假装当时已知。
5. 所有多周期数据必须使用已完成、当时可获得的数据；不得读取尚未完成的高周期未来信息。

## 2. 核心模型准入规则

1. 未建立文档化假设（`H-XXX`）的算法，不得进入核心模型。
2. 未经过可复现实验验证的模块，不得进入核心模型。
3. 新模块应先作为独立实验或可插拔增强项验证，再决定是否合并。
4. 禁止一次性把大量指标、过滤器和评分项堆入核心模型。
5. 采用“Baseline → Increment → Ablation（基线 → 增量 → 消融）”的研究路线。

## 3. 可复现性

1. 每个正式实验必须有唯一编号：`EXP-XXX`。
2. 实验必须记录：
   - 研究问题；
   - 假设；
   - 数据范围与时区；
   - 参数；
   - 代码版本或 commit；
   - 输出指标；
   - 结论与局限。
3. 不得只保存最终图表而丢失生成过程。
4. 不得静默修改默认参数、数据范围或验证口径。

## 4. 工程规则

1. `main` 代表当前稳定、可解释、已审查的项目状态。
2. 复杂研究和功能开发应使用独立 branch / worktree。
3. 研究任务不得直接修改正式 Pine 核心实现，除非任务本身是经批准的工程迁移。
4. 所有结构输出必须可解释，并能追溯到：来源周期、来源类型、原始时间、确认时间和生命周期状态。
5. 合并前必须运行与改动相关的测试或验证检查。
6. 禁止以“胜率更高”作为唯一合并理由；必须同时审查未来函数、样本外、参数敏感性和结构稳定性。

## 5. V1 产品边界

当前 V1：

- 第一研究市场：`XAUUSD`；
- 先做指标研究，不做 EA；
- 不提供买卖箭头；
- 核心结构来源：
  1. 多周期确认 Swing；
  2. 周期高低点；
  3. 历史反应型支撑 / 压力；
- 暂缓进入核心：Imbalance、Fibonacci、RSI、Volume、Momentum；
- 第一目标：在尽可能低的结构抖动下，稳定识别具有持续市场意义的多周期上下边界。

## 6. 可视化原则

- 黄色：已确认主要结构；
- 青 / 蓝：高周期参考结构；
- 绿色虚线：候选或形成中的结构，可关闭；
- 短线：局部微观结构；
- 主图只保留对主观分析真正有帮助的信息；
- 禁止用大量 HH / HL / BOS / Score / Debug 标签污染主图。

## 7. Agent 工作方式

每个 Agent 在开始任务前应：

1. 阅读本文件；
2. 明确任务边界和允许修改的目录；
3. 优先输出计划，再执行复杂改动；
4. 在结果中明确：做了什么、没做什么、测试结果、已知风险；
5. 不得把推测写成已验证事实。
