# Curated screenshots

Only a small, intentionally selected evidence set belongs here. Every file requires a matching manifest row, a stable ID, provenance, evidence value, and limitations.

Static screenshots are observations, not proof of real-time confirmation, absence of repainting, algorithm identity, or effectiveness.
