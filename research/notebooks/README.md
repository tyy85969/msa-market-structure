# Research notebooks

Notebooks are exploratory interfaces, not the sole record of a formal result. A formal notebook must document its dataset, timezone, parameters, commit, metrics, and execution order, and must be reproducible from a clean environment.

Clear transient outputs when they contain large data or sensitive paths. Promote reusable logic to approved modules only through a separate engineering task and review.
