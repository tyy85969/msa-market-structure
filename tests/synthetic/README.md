# Synthetic fixtures

This directory is reserved for small, deterministic, generated fixtures that exercise approved structure behavior without private market data.

Synthetic fixtures must document their event timeline and may not encode future information as if it were available earlier.
