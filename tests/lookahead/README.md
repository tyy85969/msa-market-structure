# Lookahead tests

Future tests in this directory will verify that OriginTime and ConfirmTime are distinct, confirmation delays are respected, incomplete higher-timeframe data is unavailable, and batch results agree with event-time replay.

Phase 0 defines only the test location and contract. It implements no structure algorithm or algorithm-specific test.
